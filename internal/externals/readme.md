# Usage

The external folder contains domain related operations to external entities such as databases, keyvalue stores, api, etc.
Each subpackage should offer an interface to interact with the external entity and a concrete implementation of the interface.

The libs folder also contains an external folder, but this one is helper utilities with this external tool.
