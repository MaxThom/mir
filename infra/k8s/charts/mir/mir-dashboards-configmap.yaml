{{- if .Values.mirPrometheus.enabled }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "mir.fullname" . }}-grafana-dashboards-mir
  labels:
    {{- include "mir.labels" . | nindent 4 }}
    grafana_dashboard: "1"
  annotations:
    grafana_folder: "Mir"
data:
  mir.json: |
{{ .Files.Get "dashboards/mir/mir.json" | indent 4 }}
#   core.json: |
# {{ .Files.Get "dashboards/mir/core.json" | indent 4 }}
#   telemetry.json: |
# {{ .Files.Get "dashboards/mir/telemetry.json" | indent 4 }}
#   command.json: |
# {{ .Files.Get "dashboards/mir/command.json" | indent 4 }}
#   configuration.json: |
# {{ .Files.Get "dashboards/mir/configuration.json" | indent 4 }}
#   eventstore.json: |
# {{ .Files.Get "dashboards/mir/eventstore.json" | indent 4 }}
{{- end }}
